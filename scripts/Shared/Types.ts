//
// Common type definitions
//

// A basic dictionary maps a key to a value
export type DictType = { [key: string]: string };

// A type used for callback actions
export type Action = () => void;
export type FunStr = (s:string) => void;
